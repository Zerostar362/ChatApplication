<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ChatApplication.Views.Settings"
             Title="Settings">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <VerticalStackLayout Grid.Column="0" Margin="10,10,10,10">
            <Label Text="ListeningPort"/>
            <Entry Text="text"/>
        </VerticalStackLayout>
        <VerticalStackLayout Grid.Column="1" Margin="10">
            <Label Text="Zatim nevim uvidim"/>
            <Entry Text="text"/>
        </VerticalStackLayout>
    </Grid>
    
</ContentPage>